[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npx prisma generate --schema=./prisma/schema.prisma",
  "npx tsc -p ./tsconfig.json"
]

[start]
cmd = "npx prisma migrate deploy --schema=./prisma/schema.prisma && node dist/index.js"
