{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "node node_modules/.bin/vite preview --host 0.0.0.0 --port ${PORT:-3000}",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE"
  }
}
